@using LinuxPackages.Web.Mvc.Infrastructure.Helpers
@using LinuxPackages.Common.Constants

@model IEnumerable<LinuxPackages.Web.Mvc.ViewModels.Packages.ListedScreenshotViewModel>

@if (Model.Count() > 0)
{
    var packageUrl = (string)ViewData["PackageUrl"];

    <p>Click to view full size.</p>
    foreach (var screenshot in Model)
    {
        <a href="/Files/Screenshots/@packageUrl/@screenshot.Url">
            <img src="/Files/Screenshots/@packageUrl/@screenshot.Url/@(PackageConstants.ScreenshotThumbnailWidth)x@(PackageConstants.ScreenshotThumbnailHeight)" class="img-responsive" />
        </a>
        <br />
    }
}
else
{
    <p>No screenshots uploaded for this package!</p>
}